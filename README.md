# SmartPension 养老智能服务机器人

当前阶段：Phase 1A（只读基线审计）

本项目是养老/智能服务机器人项目，不是授粉车 EPICar 项目。

当前目标：完成 SSH、ROS、网络和硬件基线审计，然后再进入底盘最小运动测试阶段。

架构约定：本地 `F:\AAA\SmartPension` 是唯一正式开发源；远端机器人是多队伍共用设备。后续测试如获明确许可，只能在远端独立临时目录中执行，正式代码仍保存在本地。

本首页不记录未经验证的硬件型号或 ROS 结论。当前人工确认的连接入口为 `ssh smartpension`；校园网地址 `10.120.254.216` 仅是当时确认的地址，不代表永久固定 IP。

